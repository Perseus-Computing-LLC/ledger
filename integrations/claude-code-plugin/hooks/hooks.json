{
  "description": "Record each Claude Code turn's token-usage metadata in a self-hosted Perseus Ledger instance at session end.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/ledger-meter.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
